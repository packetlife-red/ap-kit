# GitHub Pages で公開する手順

スマホから **Macなしで** ドリルを開けるようにするための手順。所要 15〜20分。

## なぜこれをやるのか

いまの方式は、スマホから使うたびに **Macでサーバを立てる必要がある**。

```bash
cd dist && python3 -m http.server 8765   # ← これを毎回やらないとスマホから開けない
```

さらに、Macを起動するたびにアドレス（`192.168.x.x`）が変わる可能性があり、
**アドレスが変わるとスマホ側には「別のサイト」に見えて、成績が消える。**

GitHub Pages に置くと、URLが**永久に固定**される。すると、

- Macを起動しなくても、スマホからいつでも開ける
- 成績が確実に残る（URLが変わらないから）
- ホーム画面に追加すれば、普通のアプリと見分けがつかない
- 一度読み込めば、機内モードでも動く（オフライン対応済み）

## 公開されるもの・されないもの

**公開される：** ドリル本体のコード・レッスン本文・README

**公開されない：**（`.gitignore` で除外済み）

| ファイル | 理由 |
|---|---|
| `しくみ.md` | 自宅のプライベートIP・グローバルIPが実測値で書かれているため |
| `HANDOFF.md` | 個人の作業メモ。機密ではないので、公開したければ `.gitignore` から消してよい |

**確認済み：** 公開物に氏名・メールアドレス・IPアドレス・パスの類は含まれていない（機械的に検査済み）。
出題は全問その場で自動生成しており、**IPA過去問の問題文は1文字も含んでいない**。

---

## 手順

### 1. GitHubのアカウントを用意する

すでに持っていれば飛ばす。持っていなければ https://github.com で無料登録する。

**ユーザー名がURLの一部になる**ので、人に見せてもよい名前にしておく。
（例：ユーザー名が `packetlife` なら `https://packetlife.github.io/ap-kit/`）

### 2. リポジトリを作る

https://github.com/new を開いて、

| 項目 | 設定 |
|---|---|
| Repository name | `ap-kit` |
| 公開設定 | **Public**（Publicでないと無料プランではPagesが使えない） |
| Add a README file | **チェックしない**（既にあるため） |
| .gitignore / license | どちらも None |

「Create repository」を押す。

### 3. Macから push する

ターミナルで以下を順に実行する。**`あなたのユーザー名` の部分だけ自分のものに置き換える。**

```bash
cd /Users/kazuya/個人/claude/260727_ap-kit
```

```bash
git init && git add . && git commit -m "応用情報キット 初回公開"
```

```bash
git branch -M main
```

```bash
git remote add origin https://github.com/あなたのユーザー名/ap-kit.git
```

```bash
git push -u origin main
```

最後のpushでユーザー名とパスワードを聞かれる。**パスワードは通常のログインパスワードではなく「アクセストークン」**が必要。
聞かれたら https://github.com/settings/tokens で「Generate new token (classic)」→ `repo` にチェック → 生成された文字列をパスワード欄に貼る。

> 補足：ブラウザで認証が完結する `gh` コマンドを使う方法もある。`brew install gh` が必要なので、上のやり方で詰まったら教えてほしい。

### 4. Pages を有効にする

1. GitHubで作ったリポジトリを開く
2. 上部の **Settings** タブ
3. 左サイドバーの **Pages**
4. 「Build and deployment」の **Source** を `Deploy from a branch` にする
5. **Branch** を `main`、フォルダを **`/dist`** にする（← ここが重要）
6. **Save**

1〜2分待つと、同じ画面の上部にURLが表示される。

```
https://あなたのユーザー名.github.io/ap-kit/
```

### 5. スマホで開く

そのURLをスマホのブラウザ（SafariでもChromeでも）で開く。

**ホーム画面に追加する：**

- iPhone（Safari）：下の共有ボタン → 「ホーム画面に追加」
- Android（Chrome）：右上の「⋮」→ 「ホーム画面に追加」

以降はアイコンから起動できる。**機内モードでも動く。**

---

## 更新するとき

中身を直したら、この3つを順に実行する。

```bash
cd /Users/kazuya/個人/claude/260727_ap-kit && python3 tools/build.py
```

```bash
git add . && git commit -m "更新内容をここに書く"
```

```bash
git push
```

1〜2分でサイトに反映される。

**スマホ側の反映：** オフライン対応の仕組み上、古い版がしばらく表示されることがある。
その場合はブラウザで**一度リロード**すれば新しい版に入れ替わる（次回起動時から反映される作り）。

---

## よくあるつまずき

**404が出る**
Pages の設定でフォルダが `/dist` になっているか確認する。`/root` のままだと `index.html` が見つからない。

**画面が真っ白**
`python3 tools/build.py` を実行し忘れて、`dist/` が古いまま push された可能性が高い。ビルドしてから push し直す。

**成績が消えた**
URLが変わると別サイト扱いになる。`https://～.github.io/ap-kit/` から常に開いているか確認する。
（`ap-kit` と `ap-kit/index.html` は同じ扱いなので、そこは問題ない）

**公開をやめたい**
Settings → Pages → Source を `None` にすれば即座に非公開になる。リポジトリ自体を削除してもよい。
