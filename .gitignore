# macOSが勝手に作るもの
.DS_Store

# 公開しないファイル
#
# しくみ.md には自宅ネットワークのプライベートIP・グローバルIPを実測値として
# 書いてあるため、公開リポジトリには入れない。
# （内容自体は無害だが、自宅の回線情報をわざわざ外に出す理由がない）
しくみ.md

# 引き継ぎメモは個人の作業記録なので公開不要。
# 公開したい場合はこの行を消す（機密は含まれていない）。
HANDOFF.md

# 科目B添削スキルは Claude Code 用の設定ファイルで、判定条件に個人名を含む。
# 本体（~/.claude/skills/）にあれば動くので、公開リポジトリには含めない。
skills/
